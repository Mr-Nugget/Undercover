import { ReplaceSpaceByDashPipe } from './replace-space-by-dash.pipe';

describe('ReplaceSpaceByDashPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSpaceByDashPipe();
    expect(pipe).toBeTruthy();
  });
});
